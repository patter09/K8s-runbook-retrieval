global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "knowledge-assistant-api"
    static_configs:
      # host.docker.internal lets the Prometheus container reach the FastAPI
      # app running on your host machine (uvicorn, port 8000).
      - targets: ["host.docker.internal:8000"]
